jwtSharedSecret: demo-secret-which-is-very-long-so-as-to-hit-the-byte-requirement

server:
  type: default
  applicationConnectors:
    - type: http
      port: 0 # random application port
  adminConnectors:
    - type: http
      port: 0 # random admin port